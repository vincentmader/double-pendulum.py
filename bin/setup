#!/bin/sh

#   Create a virtual environment for python, if it does not exist already.
    [ -d ../venv/ ] || python3 -m virtualenv ../venv/

#   Install python requirements via `pip`.
    ../venv/bin/pip3 install -r ../requirements.txt
